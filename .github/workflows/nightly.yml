name: Nightly

# The heaviest checks, too slow for every push: the long property runs and the settings batch
# (vitest's "heavy" project, vitest.config.ts), the full Python oracle, and the 100-seed pass-rate
# batches (final ≥ 98%: a seed that makes no map is breakage, PLAN §20 D145; the first-attempt
# rate is information, against its 60% target). Runs every night on dev's tip, by hand
# (gh workflow run nightly.yml --ref dev), and on a pull request that changes this file. A failure
# of the nightly or hand-started run opens an issue labelled "nightly", or comments on the open one.
#
# GitHub runs schedules from the default branch (main), so the schedule starts once this file is
# on main; it then checks dev, where work happens.

on:
  schedule:
    - cron: "41 7 * * *"
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/nightly.yml

permissions:
  contents: read

concurrency:
  group: nightly-${{ github.ref }}
  cancel-in-progress: false

env:
  # the schedule checks dev; a run by hand or on a pull request checks its own ref
  CHECK_REF: ${{ github.event_name == 'schedule' && 'dev' || '' }}

jobs:
  heavy-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ env.CHECK_REF }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: prototype/requirements.txt
      - run: npm ci
      - run: pip install -r prototype/requirements.txt
      - name: Long property runs and the settings batch
        run: npm run test:heavy

  oracle:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ env.CHECK_REF }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: prototype/requirements.txt
      - run: npm ci
      - run: pip install -r prototype/requirements.txt
      - name: Python oracle (load checks, round trip, validator parity; seeds 1-18, every theme)
        run: npm run oracle -- --seeds 1-18 --sizes 96,128,256

  batches:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ env.CHECK_REF }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # the six themes at once, one process each (the other sizes and difficulties run locally,
      # docs/progress.md)
      - name: Pass rates, every theme (100 seeds at 128² Normal, final ≥ 98%; first attempt reported)
        run: |
          mkdir -p .scratch
          pids=()
          for t in riverValley canyon highlands lakeBasin delta islands; do
            npx tsx tools/batch.ts --seeds 1-100 --size 128 --theme $t --min-first 0 > .scratch/batch-$t.txt 2>&1 &
            pids+=($!)
          done
          fail=0
          for p in "${pids[@]}"; do wait "$p" || fail=1; done
          cat .scratch/batch-*.txt
          { echo "### Pass rates, 100 seeds a theme at 128²"; echo; for t in riverValley canyon highlands lakeBasin delta islands; do sed -n '/ seeds of /,$p' .scratch/batch-$t.txt; echo; done; } >> "$GITHUB_STEP_SUMMARY"
          exit $fail

  issue:
    needs: [heavy-tests, oracle, batches]
    if: failure() && github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Open an issue
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          CHECKED: ${{ env.CHECK_REF || github.ref_name }}
        run: |
          gh label create nightly --color FBCA04 --description "The nightly checks (nightly.yml)" 2>/dev/null || true
          body="The nightly checks failed on $CHECKED: $RUN_URL"
          open=$(gh issue list --label nightly --state open --json number,title --jq 'map(select(.title == "Nightly checks failed"))[0].number // empty')
          if [ -n "$open" ]; then
            gh issue comment "$open" --body "$body"
          else
            gh issue create --title "Nightly checks failed" --label nightly --body "$body"
          fi
