# Other creators' maps, renders and per-map numbers never enter the repository (they live in
# C:\dgm-workshop). Only tools, recipes, aggregate numbers and fitted parameters are committed.
*.timber
*.png
*.jpg
*.f32
local/
ratings*.json
