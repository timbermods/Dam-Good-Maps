namespace System.Runtime.CompilerServices
{
    // Lets this netstandard2.1 assembly set the game's init-only spec properties (GameModeSpec `with`).
    internal static class IsExternalInit
    {
    }
}
